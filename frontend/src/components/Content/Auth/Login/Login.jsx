import React from 'react';
import s from './Login.module.css'

const Login = (props) => {
    return (
        <div className={s.wrapper}>
            sdfsd
        </div>
    )
}

export default Login;